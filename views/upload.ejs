<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Rudý Teror - Upload</title>
    <link rel="icon" type="image/x-icon" href="https://upload.wikimedia.org/wikipedia/en/2/2d/Rei_II.jpg">
    <style>
        body {
            margin: 0;
            font-family: Arial, Helvetica, sans-serif;
            background-image: url("https://images6.alphacoders.com/122/thumb-1920-1223901.jpg");
            background-repeat: no-repeat;
            background-position: center;
            background-size: cover;
        }

        .topnav {
            overflow: hidden;
            background-color: #000000;
        }

        .topnav a {
            float: left;
            color: #f2f2f2;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            font-size: 17px;
        }

        .topnav a:hover {
            background-color: #ddd;
            color: black;
        }

        .topnav a.active {
            background-color: #6331c0;
            color: white;
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 90vh;
            color: white;
        }

        .form-container {
            background-color: rgba(0, 0, 0, 0.7);
            padding: 20px;
            border-radius: 10px;
            width: 300px;
        }

        /* Hide the actual file input */
        input[type="file"] {
            width: 0.1px;
            height: 0.1px;
            opacity: 0;
            overflow: hidden;
            position: absolute;
            z-index: -1;
        }

        /* Style the custom file input button */
        .custom-file-input {
            padding: 10px;
            background-color: #6331c0;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            display: inline-block;
            width: 260px;
            height: 25px; /* Adjust the width as needed */
            text-align: left; /* Center the text horizontally */
        }

        /* Pseudo-element to display the text on the button */
        .custom-file-input::after {
            content: 'Vybrat soubor';
            color: white;
            display: inline-block;
            background: #6331c0;
            padding: 5px;
            white-space: nowrap;
            cursor: pointer;
            font-size: 14px;
            border: none;
            border-radius: 5px;
        }

        /* Adjust the hover effect to change the entire button color */
        .custom-file-input:hover {
            background-color: #52289f; /* Your desired hover color */
        }

        /* Adjust the hover effect for the text on the button */
        .custom-file-input:hover::after {
            background: #52289f;
        }

        input[type="submit"] {
            padding: 5px 20px;
            background-color: #00aaff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        input[type="submit"]:hover {
            background-color: #0088cc;
        }
    </style>
</head>
<body>
    <div class="topnav">
        <a href="/main">Domů</a>
        <a href="/galerie">Galerie</a>
        <a href="/dino">Motivace</a>
        <a class="active" href="/upload">Upload</a>
        <a href="/kok">Seznam Uživatelů</a>
        <a style="float: right;" href ="/profile"><%= loggedInUserName %></a>
    </div>

    <div class="container">
        <div class="form-container">
            <form action="/upload" method="post" enctype="multipart/form-data">
                <h1>Nahrát obrázek</h1>
                <label for="fileInput" class="custom-file-input"></label>
                <input type="file" id="fileInput" name="image" accept="image/*" onchange="displayFileName()" required>
                <p id="selectedFile" style="margin-bottom: 5px;"></p>
                <input type="submit" value="Nahrát obrázek" name="submit">
            </form>
        </div>
    </div>

    <script>
        function displayFileName() {
            const fileInput = document.getElementById('fileInput');
            const selectedFile = document.getElementById('selectedFile');

            if (fileInput.files.length > 0) {
                selectedFile.innerText = `Selected file: ${fileInput.files[0].name}`;
            } else {
                selectedFile.innerText = 'No file selected';
            }
        }
    </script>
</body>
</html>
