<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>users</title>
</head>
<body>
  <table>
    <% for(var i=0; i < results.length; i++) { %>
       <tr>
        <td><%= results[i].ID %></td>
         <td><%= results[i].Nazev %></td>
         <td><%= results[i].Hodnoceni %></td>
         <td><%= results[i].Rok_vydani %></td>
       </tr>
    <% } %>
    </table>
</body>
</html>